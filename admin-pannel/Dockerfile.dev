# Admin Panel Dev Container
FROM node:20-alpine

WORKDIR /app

# No npm install during build — deps install at runtime via entrypoint
EXPOSE 3001

# Entrypoint: install deps (if needed) then start dev server
CMD sh -c "npm install && npm run dev -- --host 0.0.0.0"
